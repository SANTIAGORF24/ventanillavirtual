import { Button } from "@nextui-org/react";
import React from "react";

const ButtonPrimary = ({ text }) => {
  return <Button className="bg-[#485098] font-bold text-white">{text}</Button>;
};

export default ButtonPrimary;
